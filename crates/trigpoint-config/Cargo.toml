[package]
name = "trigpoint-config"
description = "Shared triglint.toml schema, discovery, and builtin sink database"
version.workspace = true
edition.workspace = true
license-file.workspace = true
repository.workspace = true

[dependencies]
serde = { version = "1.0.219", features = ["derive"] }
thiserror = "2.0.12"
toml = "0.8.23"
